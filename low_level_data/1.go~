package main

import "fmt"

func main() {
	Number := 1223
	Byte := byte(Number)
	Back := int(Byte)
     	// byte is alias for uint8 and similarly are more -> study those and how the byte for 3 is 3 itself since the thing can store numbers from 0 to 255
	fmt.Println(Byte, Back)
}
